# Core app
streamlit==1.38.0
requests>=2.31.0
pandas>=2.2.2

# Chroma: tieni la serie 0.4.x per evitare breaking changes 0.5.x
chromadb>=0.4.22,<0.5

duckdb>=0.9.2

# OpenAI SDK recente (Responses API + compat chat)
openai>=1.40.0,<2
tiktoken>=0.7.0

# NOTA: su Streamlit Cloud non installare modelli locali
# sentence-transformers e ollama sono volutamente esclusi

# FastAPI per API
fastapi
uvicorn  
pydantic
